import { palette } from './palette';
import * as colorToken from './colorToken';

export type Palette = typeof palette;
export type ColorToken = typeof colorToken;

export default { palette, colorToken };
